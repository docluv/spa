;


(function(window, undefined) {

    var errMsg = {
        missingParameter: "parameters missing",
        invalidArgument: "invalid argument",
        indexOutOfRange: "index out of range"
    };

    return (window.errMsg = errMsg);

})(window);

